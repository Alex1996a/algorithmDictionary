10-26

class Solution:

	"""
	@param S: The set of numbers.
	@return : A list of lists. See example.

	"""

	def subsets(self, s):
	
		ret = []
		self.helper(sorted(s), ret, [])


	def helper(self, vals, ret, temp):

		ret.app(tmp[:])

		for i, val in enumerate(vals):

			self.helper(vals[i + 1], ret, tmp + [val])



