stack = []
len(stack) #size of stack

#more efficient stack

import collections
stack = collections.deque()

len(stack) != 0
stack[-1]
pop()

append(item)

Java:

Deque<Integer> stack = new ArrayDeque<Integer> ()

Stack : vector :  need synchronized


